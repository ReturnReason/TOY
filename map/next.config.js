/** @type {import('next').NextConfig} */
const nextConfig = {
  /**
 * 
 *  images: {
    // domains: ['외부 링크 사용 시 작성'],
    remotePatterns: {
      // domain 보다 더 상세하게 사용하고 싶을 경우 사용
    },
  },
 */
};

module.exports = nextConfig;
